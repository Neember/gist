.snippets-form-container
  = simple_form_for @snippet do |f|
    = f.input :title
    = f.input :status, :as => :select, :collection => Snippet.statuses.keys.to_a
    = f.association :tags,   as: :check_boxes
    = f.button :submit